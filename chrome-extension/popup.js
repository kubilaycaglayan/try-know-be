const $=id=>document.getElementById(id);const defaultApi='http://localhost:8080/api/v1';
async function request(path,options={}){const {token,apiBase}=await chrome.storage.local.get(['token','apiBase']);const r=await fetch((apiBase||defaultApi)+path,{...options,headers:{'Content-Type':'application/json',Authorization:`Bearer ${token||''}`}});if(r.status===401&&token){await chrome.storage.local.remove(['token','activeTimer']);location.reload();throw Error('Session expired')}if(!r.ok)throw Error(await r.text()||'Request failed');const text=await r.text();return text?JSON.parse(text):null}
function fillOptions(select,placeholder,values){select.replaceChildren();const empty=document.createElement('option');empty.value='';empty.textContent=placeholder;select.append(empty);values.forEach(value=>{const option=document.createElement('option');option.value=value.id;option.textContent=value.name||value.title;select.append(option)})}
async function load(){try{const paths=await request('/paths');const items=await request('/items');fillOptions($('path'),'Select a path',KnowCore.activePaths(paths));$('path').onchange=()=>fillOptions($('item'),'Select an item (optional)',KnowCore.itemsForPath(items,$('path').value));$('path').onchange();const timer=await request('/timers/current');if(timer){$('status').textContent=KnowCore.timerStatus(timer);$('toggle').textContent='Stop timer';await chrome.storage.local.set({activeTimer:timer})}else{ $('status').textContent=KnowCore.timerStatus(null);$('toggle').textContent='Start timer';await chrome.storage.local.remove('activeTimer') }$('auth').hidden=true;$('workspace').hidden=false}catch(e){$('error').textContent='Sign in failed or the API is unavailable.'}}
async function login(){try{const result=await request('/auth/login',{method:'POST',body:JSON.stringify({email:$('email').value,password:$('password').value})});await chrome.storage.local.set({token:result.token});$('error').textContent='';await load()}catch(e){$('error').textContent='Invalid credentials.'}}
$('login').onclick=login;$('logout').onclick=async()=>{await chrome.storage.local.clear();location.reload()};$('toggle').onclick=async()=>{try{const current=await request('/timers/current');if(KnowCore.timerIsRunning(current)){await request('/timers/stop',{method:'POST',body:'{}'});await chrome.storage.local.remove('activeTimer');$('status').textContent=KnowCore.timerStatus(null);$('toggle').textContent='Start timer'}else{const timer=await request('/timers',{method:'POST',body:JSON.stringify(KnowCore.timerStartPayload($('path').value,$('item').value,$('description').value))});await chrome.storage.local.set({activeTimer:timer});$('status').textContent=KnowCore.timerStatus(timer);$('toggle').textContent='Stop timer'}}catch(e){$('error').textContent='Could not update timer.'}};chrome.storage.local.get('token').then(({token})=>{if(token)load()});
$('options').onclick=()=>chrome.runtime.openOptionsPage();
