services:
  db:
    ports: !reset ["127.0.0.1:${DB_DEV_PORT:-15432}:5432"]
  api:
    build: !reset null
    image: gradle:8.13-jdk21
    working_dir: /app
    command:
      - sh
      - -c
      - |
        ./gradlew classes --no-daemon
        ./gradlew classes --continuous --no-daemon &
        compiler_pid=$$!
        trap 'kill "$$compiler_pid" 2>/dev/null || true' EXIT
        exec ./gradlew bootRun --no-daemon
    ports: !reset ["127.0.0.1:${API_DEV_PORT:-18081}:8080"]
    volumes:
      - ./backend:/app
      - gradle-cache:/home/gradle/.gradle
  web:
    build: !reset null
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm ci && npm run dev"
    environment:
      VITE_GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID:-}
    volumes:
      - ./frontend:/app
      - frontend-node-modules:/app/node_modules
    expose: ["5177"]
  proxy:
    ports: !reset ["127.0.0.1:${PROXY_DEV_PORT:-3000}:80"]
    environment:
      WEB_UPSTREAM: web:5177

volumes:
  gradle-cache: {}
  frontend-node-modules: {}
