 I don't see any important batches on the import batches page, but actually, I did two different imports previously this was before we implemented the import batches pages, but I expect to see them nevertheless because we should be keeping the imports in the database as import batches so for now I don't see the imported batches on the import batches page.

Proof: implemented in commit dc61b69.
