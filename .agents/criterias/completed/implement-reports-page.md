We need to implement a detailed report page to show a timeline to make visible, which paths for taking the time day by day. It should have the week, months, and year views, it should have categories paths and other entities. You can completely inspire from Clockify.

Implemented in commit `074a6db` (`feat: add time reports page`).
