 for the paths that we are using in across the application whenever we are listing the paths, we need to sort down, depending on their last used date so for example, if recently a user created a job path session and then another session so the first should be the last session and second should be the second used path in the sorting. An example of sorting the past is when we are starting a session we are choosing a pats another example of sorting the path is the path stage.. sort starting from the last used path.

#### PROOF
Implemented in commit: 3285209
