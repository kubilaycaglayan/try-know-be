we should enable hot relading for the development environment so the developers can see the implemented changes. If we don't have it at the moment, implement it.
Please note that the dev env meant to be acceses from another machine via ssh forwarding. So the app should be running in a rare but rememberable port. The port should be noted to the developer during the development run.
After the changes we should also update the related skill in the .agents/skills directory.
