Users should be able to import data from Clockify.
An example classify data type can be found in directory data/clockify-july.json
Analyze this file and create a functionality for users to be able to paste this kind of data objects so that the application can convert the time series into its own objects for that user.
A user should be able to navigate to a feature in the dashboard such as import data from classify, and when they click that button, that could be something like a textbook, but the context should be very big so that the big objects can fit inside and when user paste the content inside the text box or context box and hit upload the application should be able to take care of the rest.
You should check the file data/clockify-july.json and create mapping from collective objects to our own objects for this specific feature.
If there is no entities that exist in clarify, but not exist in the users data we should create that entity, such as if the user doesn't have a specific path we should create it.
